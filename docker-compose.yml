services:
  postgres:
    image: postgres:16-alpine
    container_name: teste-analitica-postgres
    environment:
      POSTGRES_USER: analitica
      POSTGRES_PASSWORD: analitica
      POSTGRES_DB: fleet_test
    ports:
      # 5433 no host: evita conflito com instalações locais de Postgres,
      # comuns em máquina de desenvolvimento, que já ocupam a porta padrão 5432.
      - '5433:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U analitica -d fleet_test']
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
